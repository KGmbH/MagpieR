% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trial_format.R
\name{trial_format}
\alias{trial_format}
\title{Build magpie trials (experimental items + optional fillers)}
\usage{
trial_format(
  n_exp,
  fillers = NULL,
  coin_var = "coin",
  question = "How acceptable is the utterance?",
  n_options = 0L,
  option_labels = NULL,
  js_var = "trial_template",
  print_js = TRUE
)
}
\arguments{
\item{n_exp}{Number of experimental items (Item1, Item2, …).}

\item{fillers}{Character vector of filler sentences (optional).
Each string is inserted verbatim into the \emph{sentence}
field and \strong{never} wrapped in \verb{''.concat(...)}.}

\item{coin_var}{JS variable that indexes the condition
(\code{syn.Item1[coin]}). Default \code{"coin"}.}

\item{question}{Prompt shown to participants (Likert task).
Default \emph{“How acceptable is the utterance?”}.}

\item{n_options}{0 = Likert task (default); 2 or 3 = forced‑choice.}

\item{option_labels}{\emph{When \code{n_options == 0}} → ignored.
\emph{When \code{n_options == 2}} and \strong{missing} → defaults to
\verb{c("Yes", "No")}.
Otherwise \strong{must} be either
• a vector of length \code{n_options} (same labels for every item), or
• a list of \code{n_options} vectors. The \emph{i‑th} element of each vector is
used for item \emph{i} (cycling if shorter than the number of items).}

\item{js_var}{JS variable name for the emitted array
(default \code{"trial_template"}).}

\item{print_js}{Logical. Write JS to the console?  Default \code{TRUE}.}
}
\value{
(invisibly) list of trial objects.
}
\description{
Build magpie trials (experimental items + optional fillers)
}
